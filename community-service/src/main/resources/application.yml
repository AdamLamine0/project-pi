server:
  port: 8082

spring:
  application:
    name: community-service

  mongodb:
    uri: mongodb+srv://malekbaouindi_db_user:citPjISEKQHd0Klz@cluster0.wpulfjf.mongodb.net/community_db?appName=Cluster0

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
    instance-id: ${spring.application.name}:${server.port}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha